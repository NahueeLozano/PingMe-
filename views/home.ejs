<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>PingMe 🐷</title>
  <link rel="stylesheet" href="/home.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body>

<!-- Header -->
<header class="pingme-header d-flex justify-content-between align-items-center px-3 py-2 bg-pink text-white">
  <h1 class="m-0">PingMe 🐷</h1>
  <nav class="nav-right d-flex gap-3">
    <button><i class="bi bi-emoji-smile"></i></button>
    <button><i class="bi bi-house-fill"></i></button>
    <button><i class="bi bi-people-fill"></i></button>
    <button><i class="bi bi-bell-fill"></i></button>
    <button><i class="bi bi-grid-3x3-gap-fill"></i></button>
    <button><i class="bi bi-chat-dots-fill"></i></button>
    <button><i class="bi bi-search"></i></button>
  </nav>
</header>

<!-- Panel lateral fijo a la izquierda -->
<aside class="contactos-activos">
  <h4 class="text-pink mb-3">Notificaciones</h4>
  <ul class="notificaciones list-unstyled mb-4">
    <li><i class="bi bi-person-plus-fill text-success me-2"></i></li>
    <li><i class="bi bi-gift-fill text-warning me-2"></i></li>
    <li><i class="bi bi-chat-dots-fill text-primary me-2"></i></li>
    <li><i class="bi bi-heart-fill text-danger me-2"></i></li>
  </ul>

  <h4 class="text-pink mb-3">Conectados</h4>
  <ul class="list-unstyled">
    <li></li>
  </ul>

  <h4 class="text-pink mt-4 mb-3">Configuración</h4>
  <div class="d-flex flex-column gap-2">
    <button class="btn btn-outline-danger btn-sm text-start">
      <i class="bi bi-box-arrow-right me-1"></i> Cerrar sesión
    </button>
  </div>
</aside>

<!-- Layout principal -->
<div class="container-fluid mt-4">
  <div class="row">
    <!-- Contenido principal desplazado -->
    <main class="col-12 col-md-8 contenido-principal">
      <form action="/agregarPost" method="POST" class="mb-4 p-3 rounded" style="background:#ffd6eb;">
        <div class="d-flex align-items-center mb-2">
          <img src="https://via.placeholder.com/40" class="rounded-circle me-2" alt="Perfil">
          <strong>Nahuel Lozano</strong>
        </div>

        <textarea name="contenido" rows="3" class="form-control mb-3" placeholder="¿Qué estás pensando, Nahuel?" required></textarea>

        <div class="d-flex gap-3 mb-3 text-secondary">
          <div><i class="bi bi-image text-success me-1"></i> Foto/video</div>
          <div><i class="bi bi-emoji-smile text-warning me-1"></i> Sentimiento/actividad</div>
          <div><i class="bi bi-tag text-primary me-1"></i> Etiqueta amigos</div>
        </div>

        <div class="text-end">
          <button type="submit" class="btn-pink">Publicar</button>

        </div>
      </form>

      <% if(posts && posts.length > 0) { %>
        <% posts.reverse().forEach(post => { %>
          <div class="post mb-4 p-3 rounded" style="background:#ffd6eb;">
            <h3 class="text-pink"><%= post.nombre %> <%= post.apellido %></h3>
            <p><%= post.contenido %></p>
            <small class="text-muted"><%= post.fecha %></small>
            <div class="actions mt-2 d-flex gap-3">
              <button class="btn btn-outline-success btn-sm">
                <i class="bi bi-hand-thumbs-up-fill me-1"></i> Me gusta (<%= post.likes.length %>)
              </button>
              <button class="btn btn-outline-primary btn-sm">
                <i class="bi bi-chat-left-text-fill me-1"></i> Comentar (<%= post.comentarios.length %>)
              </button>
              <button class="btn btn-outline-warning btn-sm">
                <i class="bi bi-share-fill me-1"></i> Compartir
              </button>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <p class="text-center">No hay posts todavía</p>
      <% } %>
    </main>
  </div>
</div>

<!-- Footer -->
<footer class="text-center bg-pink text-white py-3 mt-4">
  <p class="mb-0">&copy; 2024 PingMe. Todos los derechos reservados.</p>
</footer>

</body>
</html>


